import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seeweather.rezkywm',
  appName: 'metric_converter-rezkywm-ionic_vue',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
